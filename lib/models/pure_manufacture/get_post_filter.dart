/// This a Pure Manufacture class that is used to filter the getting of a post.
class GetPostFilter {
  /// The id of the post.
  final int id;

  /// Creates a [GetPostFilter] with the given [id].
  GetPostFilter({required this.id});
}
